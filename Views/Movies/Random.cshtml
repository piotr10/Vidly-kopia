@model IEnumerable<Vidly.Models.Movie>

@{
    ViewBag.Title = "Movies";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Movies</h2>
<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Movie</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var movie in Model)
        {
            <tr>
                <td>@movie.Name</td>
            </tr>
        }
    </tbody>
</table>





@*
    @model Vidly.ViewModel.RandomMovieViewModel
    @{
        ViewBag.Title = "Random";
        Layout = "~/Views/Shared/_Layout.cshtml";
    }

    @*
        klasa się wyświetli jeżeli film został wyporzyczony przez co najmniej 5 klientów jeżeli nie to null i klasa popular nie zostanie wyrenderowana
*@

@*
    @{
        var className = Model.Customers.Count > 5 ? "popular" : null;
    }

    <h2 class="@className">@Model.Movie.Name</h2>
    @Model.Customers

    @*
        if który wyswietli użytkownikowy że filmy nikt wczesniej nie wypożyczył tego filmu i jako pierwszy klient dostanie taką wiadmość
*@

@*
    @if (Model.Customers.Count == 0)
    {
        <text>No one has rented this movie before</text>
    }
    else
    {
        <ul>
            @foreach (var customer in Model.Customers)
            {
                <li>@customer.Name</li>
            }
        </ul>
    }
*@